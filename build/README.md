# Build Assets

This directory holds optional installer assets used by `electron-builder`.

## Common additions

- `icon.ico` for Windows installers
- `icon.png` for Linux packages
- custom NSIS resources if Apollo later needs branded installer chrome
